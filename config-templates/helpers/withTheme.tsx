/*
import { MuiThemeProvider } from '@material-ui/core'
import mainTheme from '..<set path to>/mainTheme'
import React from 'react'

const withTheme = <P extends object>(Component: React.ComponentType<P>) => (props: P) => (
  <MuiThemeProvider theme={mainTheme}>
    <Component {...props} />
  </MuiThemeProvider>
)

export default withTheme
*/
